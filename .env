# This file is for local development secrets and configuration.
# DO NOT commit this file to version control.

# The database connection string. For local development, we use SQLite.
# For production, this will be replaced with the PostgreSQL connection string.
DATABASE_URL="sqlite:///./life.db"

# Google OAuth 2.0 Credentials
# Replace these with the actual values from your Google Cloud project.
GOOGLE_CLIENT_ID="YOUR_GOOGLE_CLIENT_ID"
GOOGLE_CLIENT_SECRET="YOUR_GOOGLE_CLIENT_SECRET"

# The redirect URI must match exactly what you configured in the Google Cloud Console.
REDIRECT_URI="http://127.0.0.1:8000/api/auth/google/callback"